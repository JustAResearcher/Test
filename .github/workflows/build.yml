name: build-meowcoin

on:
  push:
    branches: [ upgrade/remove-asset-layer ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential pkg-config automake libtool autoconf autotools-dev \ 
          libevent-dev libboost-system-dev libboost-filesystem-dev libboost-chrono-dev libboost-test-dev \ 
          libboost-thread-dev libssl-dev libminiupnpc-dev libzmq5-dev python3 python3-pip git curl ca-certificates \ 
          libdb5.3-dev libdb5.3++-dev

    - name: Configure & build
      run: |
        set -eux
        ./autogen.sh || true
        ./configure || true
        make -j2 2>&1 | tee build.log || true

    - name: Upload build log
      uses: actions/upload-artifact@v4
      with:
        name: build-log
        path: build.log

